import 'package:flutter/material.dart';

class MealGridItem extends StatelessWidget {
  const MealGridItem({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
